// Types de configuration:
// app

const appConfig = {
  route: {
    welcome: 'welcome',
    loginSuccess: 'videos-add',
    loginError: 'unauthorized',
    logout: 'welcome'
  }
};

export default {
  app: appConfig
};
